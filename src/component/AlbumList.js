import React, {Component} from 'react';
import {
    View,
    Text,
} from 'react-native';


class Albumlist extends Component{
    return (
        <View> 
            <Text>Albumlist</Text>
        </View>
    );
};

export default Albumlist;